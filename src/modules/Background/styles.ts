import { StyleSheet, Dimensions } from 'react-native';

const styles = StyleSheet.create({
  background: {
    flex:1,
    paddingTop: 150,
    width: '100%',
    height:'100%'
  }
})

export default styles