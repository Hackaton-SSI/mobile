import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: 220,
    height: 270,
    backgroundColor: '#fff',
    borderRadius:25
  },

  image: {
    width: 100,
    height: 100
  }
})

export default styles